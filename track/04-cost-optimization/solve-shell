#!/bin/bash
# solve-shell for Exercise 4: Cost Optimization

EXERCISE_DIR="/home/user/exercise"
SOLUTION_DIR="/home/user/solution"

echo "Applying solution for Exercise 4..."

cp -r "$SOLUTION_DIR/src/." "$EXERCISE_DIR/src/"

cd "$EXERCISE_DIR"
mvn compile -q

echo ""
echo "✅ Solution applied."
echo "   Review: src/main/java/fulfillment/FulfillmentWorkflowImpl.java"
echo "   Key changes:"
echo "     - LocalActivityOptions with 5s timeout (not ActivityOptions)"
echo "     - Workflow.newLocalActivityStub() (not newActivityStub)"
echo "     - localActivities.validateOrder() and .fraudCheck() before child workflow"
echo ""
echo "   Terminal 1: mvn compile exec:java -Dexec.mainClass=\"fulfillment.FulfillmentWorker\""
echo "   Terminal 2: mvn exec:java -Dexec.mainClass=\"fulfillment.Starter\""
echo "   Compare MarkerRecorded vs. ActivityTask triplets in the Web UI."
